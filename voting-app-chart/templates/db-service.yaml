apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: db
spec:
  type: ClusterIP
  ports:
  - name: "db-service"
    port: {{ .Values.db.port }}
    targetPort: {{ .Values.db.port }}
  selector:
    app: db
